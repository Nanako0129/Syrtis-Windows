# Rust
/target/

# .NET
src/**/bin/
src/**/obj/
*.user

# macOS noise
.DS_Store

# Local agent guide stays untracked (same convention as the macOS repo)
CLAUDE.md
AGENTS.md

# Agent handover notes and session scratch. Both were already excluded, but
# only in .git/info/exclude, which is per-clone and never committed - so this
# checkout was protected and a fresh clone, another machine and CI were not.
.agent-local/
scratchpad/
